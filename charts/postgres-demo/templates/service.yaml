apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-demo.fullname" . }}
  labels:
    {{- include "postgres-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "postgres-demo.selectorLabels" . | nindent 4 }}
