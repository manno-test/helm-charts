apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-demo.fullname" . }}
  labels:
    {{- include "redis-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "redis-demo.selectorLabels" . | nindent 4 }}
