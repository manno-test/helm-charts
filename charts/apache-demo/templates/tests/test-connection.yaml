apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apache-demo.fullname" . }}-test-connection"
  labels:
    {{- include "apache-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apache-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
