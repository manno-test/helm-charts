apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "busybox-demo.fullname" . }}-test-connection"
  labels:
    {{- include "busybox-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "busybox-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
